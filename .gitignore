# Gradle / Kotlin
.gradle/
build/
.kotlin/
local.properties

# Android
*.apk
*.aab
.cxx/

# Upstream hack.chat server source — its own clone of
# https://github.com/hack-chat/main, kept here as a protocol reference.
# See README.md for how to obtain it.
/hc/

# Probe state: mirrors the server's per-address rate score between runs.
probe/.governor.json

# `ws` for probe/fakeserver.mjs; installed with --no-save, see probe/README.
node_modules/

# Editors / OS
.idea/
*.iml
.DS_Store

# Release signing key — never committed. See docs/releasing.md.
*.jks
*.keystore
